/// <reference path="./express.d.ts" />

// This module ensures our global Express type augmentations are loaded when running under ts-node.
// The actual augmentation is declared in `express.d.ts`, which is a type declaration file.

export {};